# Streamlit Secrets Configuration Example
# Copy this to .streamlit/secrets.toml and fill in your values
# For Streamlit Cloud, add these as secrets in the app settings

# API Gateway URL (for cloud deployment, use your deployed API URL)
API_BASE_URL = "http://127.0.0.1:8000"

# Default credentials (change these in production!)
DEFAULT_USERNAME = "enterprise_admin"
DEFAULT_PASSWORD = "admin123"

